# 🏇 公営競技 予想アプリ集

ブラウザだけで動く自動予想 Web アプリ。PWA対応でスマホのホーム画面にも追加できます。

| アプリ | URL | データ源 | バックエンド |
| --- | --- | --- | --- |
| 🐎 **中央競馬AI予想** | [`keiba.html`](keiba.html) | netkeiba（公開オッズ/出走表） | 要・データサーバー（無料Worker） |
| 🚤 ボートレース予想 | [`index.html`](index.html) | Boatrace Open API | 不要（直前情報のみ任意Worker） |

---

# 🐎 中央競馬AI予想アプリ

JRA（中央競馬）の**本日の全レース**について、**市場オッズから控除率（過剰率）を取り除いた
厳密な勝率**を基準に、Plackett–Luce の閉形式で **単勝/複勝/馬連/ワイド/馬単/3連複/3連単**の
的中確率を数式で正確に計算し、**「勝ちやすい買い目」**を自動提案する Web アプリです。

## 特長

- **本日の全開催・全レースを自動取得** — netkeiba の公開オッズ・出走表から、開催場とレースを選ぶだけ。
- **市場オッズ基準の厳密確率** — オッズの逆数を正規化して控除率を除いた「市場の勝率」を基準に、
  さらに競馬で実証されている**本命-大穴バイアス（favorite-longshot）補正**を加えた予想勝率を算出。
- **人間に解けない多点の的中率を数式で** — 複勝・ワイド・馬連・3連複・3連単の的中確率を
  Plackett–Luce の**閉形式（乱数・シミュレーション不使用）**で正確に計算。
- **勝ちやすい買い目を提案** — 本命の複勝、上位3頭ワイドBOX、馬連◎流し、3連複◎軸流し、
  3連単フォーメーション（点数あたり的中率が最大になる効率フロンティア）を、
  **的中率・点数・金額つき**で提示。
- **PWA** — ホーム画面に追加してアプリのように利用可能。

## 使い方

1. **一度だけ**データサーバー（Cloudflare Workers / 無料）を用意します
   → 手順は [`worker/KEIBA-README.md`](worker/KEIBA-README.md)（`keiba-proxy.js` を貼り付けるだけ）。
2. アプリの **⚙️データサーバー設定** に Worker の URL を貼り付け（保存されます）。
3. 「開催」と「レース」を選んで「**予想する**」。

> なぜサーバーが要るの？ 中央競馬には**無料の公式JSON APIが無く**、netkeiba 等は
> CORS 非対応のためブラウザから直接読めません。そこで公開データを JSON 化して中継する
> 小さな無料サーバー（Worker）を一度だけ用意します。

## 仕組み（要点）

1. **市場勝率** `p_i = (1/オッズ_i) / Σ(1/オッズ_j)` … 控除率を除いた市場の主観勝率。
2. **バイアス補正** `w_i ∝ p_i^β`（β=1.15）… 本命の過小評価／大穴の過剰人気を補正。
3. **厳密確率** Plackett–Luce 閉形式で各馬の勝率・連対率・複勝率、各券種の的中確率を計算。
4. **買い目最適化** 点数あたり的中率が最大になるフォーメーション（効率フロンティア）を提案。

各パラメータ（`BETA` ほか）は `keiba.js` 冒頭で調整できます。

## 注意

本アプリの予想は公開オッズ等に基づく自動計算であり、的中・利益を保証するものではありません。
馬券の購入は自己責任で行ってください。**20歳未満は馬券を購入できません。**

データ取得元: [netkeiba](https://race.netkeiba.com/)

---

# 🚤 ボートレース予想アプリ

[Boatrace Open API](https://boatraceopenapi.github.io/) の本日の出走表データを使い、
ブラウザだけで動く（バックエンド不要の）ボートレース自動予想 Web アプリです。
スマホでもそのまま使え、ホーム画面に追加すればアプリのように起動できます（PWA対応）。

## 特長

- **インストール不要** — URL を開くだけ。スマホ／PC 両対応のレスポンシブUI。
- **本日のレースを自動取得** — 開催中の全24場・各レースの出走表をリアルタイム取得。
- **直前情報を反映** — **実際の進入コース・展示タイム・展示ST・チルト・天候/風/波/気温/水温**を
  予想に取り込みます。特に進入隊形の変化（前付け）と展示タイムは的中に直結。
  - 締切前の展示は、付属の **直前情報サーバー（Cloudflare Workers / `worker/`）** を無料デプロイして
    アプリの ⚙️ に登録すると、公式サイトから自動取得します（オープンAPIは過去レースのみ反映のため）。
- **Claude流の分析予想** — 各艇のコース取り（戦法）と全指標を分析し、◎○▲△の印と
  「なぜそう読むか」の所見コメント、レース総評を自動生成。
- **厳密な確率計算（乱数なし）** — Plackett–Luce の閉形式で各艇の1着/2着/3着確率と
  3連単(1着-2着-3着)の確率を数式で正確に算出します（モンテカルロ抽選は不使用）。
- **勝ちに最も近いフォーメーション** — 算出した3連単確率から、点数あたりの的中率が
  最大になる買い目（効率フロンティア）を「絞り／標準／手広く」で提案。各案の的中率と
  必要金額を表示します。
- **PWA** — ホーム画面に追加してフルスクリーンのアプリとして利用可能。

## 使い方

1. 公開 URL（GitHub Pages）をスマホ／PCのブラウザで開く
2. 「会場」と「レース」を選ぶ
3. 「予想する」をタップ

## 公開方法（GitHub Pages）

このリポジトリには Pages 自動デプロイ用のワークフロー（`.github/workflows/deploy.yml`）が含まれています。

- 対象ブランチ（`claude/zen-hawking-0ed2tx` または `main`）に push すると自動でビルド・公開されます。
- 初回は GitHub の **Settings → Pages → Build and deployment → Source** を **「GitHub Actions」** に設定してください
  （ワークフロー側でも `enablement: true` により自動有効化を試みます）。
- 公開 URL は `https://<ユーザー名>.github.io/<リポジトリ名>/` です。

## 仕組み

**0. 直前情報の取得** — `previews/v2/today.json` から進入コース・展示タイム・展示ST・
チルト・気象を取得（あれば）。**枠番ではなく実際の進入コース**で有利度を評価し、
**展示タイム**をレース内で相対評価して加点、展示STの鋭さも反映します。

**1. 各艇の強さ（スコア）を算出** — 出走表のあらゆる指標を重み付け（合計1.0）:

| 指標 | フィールド | 重み |
| --- | --- | --- |
| コース | 進入コース別の全国平均1着率（イン有利を反映） | 0.26 |
| 全国2連対率 | `racer_national_top_2_percent` | 0.18 |
| 全国勝率 | `racer_national_top_1_percent` | 0.10 |
| 全国3連対率 | `racer_national_top_3_percent` | 0.04 |
| 当地2連対率 | `racer_local_top_2_percent` | 0.08 |
| 当地勝率 | `racer_local_top_1_percent` | 0.04 |
| 平均ST | `racer_average_start_timing`（低いほど高評価） | 0.10 |
| モーター2連対率 | `racer_assigned_motor_top_2_percent` | 0.08 |
| モーター3連対率 | `racer_assigned_motor_top_3_percent` | 0.03 |
| ボート2連対率 | `racer_assigned_boat_top_2_percent` | 0.03 |
| 級別 | A1/A2/B1/B2 | 0.06 |

F（フライング）・L（出遅れ）回数は減点。

**2. 確率を厳密に計算** — 強さ `w=exp(score/TEMP)` を用い、
[Plackett–Luce モデル](https://en.wikipedia.org/wiki/Discrete_choice)の**閉形式**で
各艇の1着/2着/3着確率と3連単(1着-2着-3着)の確率を計算します（乱数・シミュレーションは不使用）。

**3. 分析コメント生成** — コース取り・級別・全国/当地成績・ST・モーターなどから
各艇の所見とレース総評を文章で生成します。

**4. 最適フォーメーション選択** — 全候補フォーメーションを算出確率で評価し、
点数あたりの的中率が最大になる「効率フロンティア」から3段階を提案します。

各パラメータ（`WEIGHTS`・`TEMP`）は `app.js` 冒頭で調整できます。

## 注意

本アプリの予想は公開データに基づく自動計算であり、的中を保証するものではありません。
舟券の購入は自己責任で行ってください。**20歳未満は舟券を購入できません。**

データ提供: [Boatrace Open API](https://boatraceopenapi.github.io/)
